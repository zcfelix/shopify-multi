spring:
  application:
    name: price-service
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL57Dialect
    open-in-view: false
  output:
    ansi:
      enabled: always
